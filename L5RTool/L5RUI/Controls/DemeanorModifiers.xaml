<UserControl x:Class="L5RUI.Controls.DemeanorModifiers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:L5RUI.Converters"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             x:Name="modifiersRoot">

    <UserControl.Resources>
        <conv:NameToImageConverter x:Key="imageConverter"/>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal">
        <ItemsControl>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding Path=Ring, Converter={StaticResource imageConverter}, ConverterParameter=Rings}" MaxWidth="16" MaxHeight="16" Margin="0 0 3 0"/>
                        <xctk:IntegerUpDown Minimum="-5" Maximum="5" Value="{Binding Value}"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ComboBox/>
        <Button/>
    </StackPanel>
</UserControl>
    
